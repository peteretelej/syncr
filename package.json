{
  "name": "syncr",
  "version": "1.0.0",
  "description": "Lightweight folder sync tool using rclone, Bun and pm2",
  "main": "src/cli.ts",
  "bin": {
    "syncr": "src/cli.ts"
  },
  "scripts": {
    "syncr": "bun run src/cli.ts",
    "start": "bun run src/cli.ts",
    "build": "bun build src/cli.ts --compile --outfile dist/syncr",
    "test": "bun test"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.2"
  }
}
